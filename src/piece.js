const Piece = (starting) => {
    //TODO: creating piece DOM
    //TODO: return DOM element
    let currentPosition = starting;

    const moveTo = (endingPosition) => {
        //TODO update DOM element positon
        currentPosition = ending;
    }
    return moveTo;
}
export default Piece;

